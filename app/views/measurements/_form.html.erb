<%= form_with(model: measurement) do |form| %>
  <% if measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
        <% measurement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :timestamp %>
    <%= form.datetime_select :timestamp %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>
    <div class="field">
        <%= form.label :sensor_Id %>
        <%= form.text_field :sensor_id %>
    </div>

    <div class="field">
        <%= form.label :sensorURI %>
        <%= form.text_field :sensor_uri %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<p id="notice"><%= notice %></p>

<p>
    <strong>Sensor URI:</strong>
    <%= @measurement.sensor.URI %>
</p>

<p>
  <strong>Timestamp:</strong>
  <%= @measurement.timestamp.in_time_zone("Paris") %>
</p>

<p>
  <strong>Value:</strong>
  <%= @measurement.value %>
</p>

<%= link_to 'Edit', edit_sensor_measurement_path(@sensor, @measurement) %> |
<%= link_to 'Back', sensor_measurements_path(@sensor) %>
